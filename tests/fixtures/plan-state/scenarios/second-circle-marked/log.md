# Результати перебудови

## Знижка за купоном має одного власника

- Результат: `pricing.coupon_discount` — єдина умова `SAVE10`; `order_total` і `receipt_lines` її використовують.
- Перевірено: `python3 -m unittest` → 4 тести OK.

## Вартість доставки має одного власника

- Результат: `pricing.shipping_cost` — єдиний поріг доставки.
- Перевірено: `python3 -m unittest` → 4 тести OK.

## Незалежний перегляд коду

- Результат: помічник `restrukt-worker` без контексту розмови переглянув код після двох перших задач: одна знахідка — `receipt.py` дублює формулу підсумку. Поставлено задачу «Підсумок товарів має одного власника».

## Підсумок товарів має одного власника

- Результат: `pricing.subtotal` — єдина формула підсумку; `receipt_lines` її використовує.
- Перевірено: `python3 -m unittest` → 4 тести OK.

## Перевірка зміненого

- Результат: новий помічник перевірив виправлення та його вплив: регресій немає; одна знахідка — `coupon_discount` повертає суму після знижки, назва неправдива. Поставлено задачу «Назва знижки правдива».
