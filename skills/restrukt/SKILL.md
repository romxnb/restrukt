---
name: restrukt
description: >-
  Перебудовує застосунок у зрозумілий, цілісний код із перевіреною поведінкою. plan створює план для незалежних виконавців; apply реалізує одну задачу; review перевіряє план або цілу систему; refine доопрацьовує імена чи зв'язаність; status показує стан. Без режиму — plan. Працює в Claude Code і Codex.
---

# restrukt

Процес: планувальник створює задум; новий агент виконує кожну задачу; окремий рев'ювер перевіряє ціле. Пам'ять між викликами — план і журнал. Самостійний агент поточного виклику відповідає за свій результат; це не обов'язково автор плану.

## Цільовий дизайн

Поточний код доводить поведінку, обмеження й інтеграції, але не правильність його класів, шарів, назв і залежностей. Виводь модель із задачі та інваріантів; стару межу зберігай за незалежним обґрунтуванням. Оптимізуй кінцеву систему в межах потрібної поведінки й реальних обмежень міграції, а не розмір diff.

## Режими

| Режим | Результат | Довідник |
|---|---|---|
| без режиму або `plan [обсяг]` | план без змін продуктового коду й тестів | [plan](references/plan.md) |
| `apply [задача]` | одна реалізована й перевірена задача | [apply](references/apply.md) |
| `review [plan\|code]` | перевірений план або фінальний перегляд коду; типово `code` | [review](references/review.md) |
| `refine [names\|coupling] [обсяг]` | один цільовий прохід | [refine](references/refine.md) |
| `status` | стан без змін файлів | [status](references/status.md) |

Claude Code: `/restrukt:plan`, `/restrukt:apply`, `/restrukt:review`, `/restrukt:refine`, `/restrukt:status`. Codex: `$restrukt` із цими аргументами. Шляхи відносні до цього файла; корінь плагіна — два рівні вище. Читай лише довідник режиму й методи на вказаному кроці; уже засвоєне та першоджерела без потреби не завантажуй.

## Повноваження

- Поведінка, сумісність і явні обмеження користувача задають рамки. Невідомий потрібний результат або дія поза дозволом потребують конкретного питання; незалежну роботу в межах поточної задачі продовжуй.
- Архітектурне рішення агента можна переглянути за доказом, оновивши план і залежні задачі. Погодження плану не фіксує всіх деталей; явно зафіксована вимога до структури лишається обмеженням.
- Внутрішні типи, функції, імена й файли обирай самостійно. `apply` дозволяє потрібні внутрішні зміни однієї задачі; `refine` — лише свого фокуса. `plan` і `review` змінюють робочі документи, але не реалізацію чи тести.

Рішення користувача записуй із наслідком, обсягом і джерелом; інший наслідок не успадковує дозволу. Вимогу спершу погодити архітектуру виконай перед реалізацією. Бережи сторонні правки, не відновлюй цілий файл заради скасування власної зміни. Вихід за обсяг обмежуй необхідними викликачами й інтеграцією та пояснюй у плані. Коміт, публікація й міграція реальних даних потребують доручення.

## Пам'ять і передача роботи

`docs/restrukt/plan.md` — єдине місце правил, рішень, контрактів задач і поточного стану; `docs/restrukt/log.md` — результати й докази перевірок. Новий агент звіряє потрібні висновки з актуальним кодом і diff, навіть за того самого commit. Непорушені висновки повторно не досліджуй. Для іншого обсягу чи старого формату застосуй [перенесення](references/continuation.md).

Зміна поведінки чи контракту завершеної задачі повертає її до перевірки; зміна перевіреного коду або підстав фінального висновку скидає «Перевірку цілого» до `очікує`. Старий запис журналу не доводить актуальність. Усі задачі `готово` ще не означає, що ціле перевірене.

Не створюй окремих атласів, рецептів, таблиць імен чи файлів на кожну задачу. При перешкоді або вичерпанні бюджету збережи незавершені правки, точку продовження й наступну перевірку; неперевірене не заверши. Типово працюй сам; перед делегуванням читай [runtime](references/runtime.md). Бюджет спільний для всього виклику й помічників.

## Відповідь

Мова користувача; імена в коді — мова проєкту. Стисло: результат, де читати сценарій і відповідальних модулів, суттєві рішення та зміни поведінки, фактичні перевірки, залишок і наступна дія. Деталі давай посиланнями. Не копіюй план, не видавай перейменування чи зелену збірку за якість архітектури й не вигадуй вимірювань витрат.
