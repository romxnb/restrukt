# План restrukt: <проєкт>

<дата> · N файлів · N точок входу · N сценаріїв · N контекстів · N пропозицій · тестів N · час: оркестратор N хв · сканери N хв · злиття N хв

## Шляхи

| шлях | що входить | рядків |
|---|---|---|
| A | слова і події всіх контекстів | n |
| B | C1 цілком | n |
| C | усе: прогін 1 — C1; прогін 2 — C2, C3 | n + n |

## Питання до тебе

| # | контекст | питання | варіанти | відповідь |
|---|---|---|---|---|
| Q1 | C1 | `session` означає авдіосесію і сеанс користувача. Розвести? | a `audioSession` + `userVisit` · b лишити одне слово | |

## Контексти

Контекст без підконтекстів — один рядок; з підконтекстами — рядок на кожен, словник спільний на контекст.

| id | назва | маршрути | файли | пропозицій |
|---|---|---|---|---|
| C1 | просторовий звук: спільні слова session, standby, renderer | | | |
| C1a | вмикання просторового звуку | R1 користувач вмикає звук · R2 застосунок сам вмикає при старті | `AppModel.swift`, `SpatialSessionController.swift` | 14 |
| C1b | відтворення | R6 тап штовхає семпли | `SpatialRenderer.swift` | 9 |
| C2 | ліцензія | R9 активація ключа | `LicenseManager.swift` | 5 |

## Джерела мови

`CLAUDE.md` · `CONTEXT.md` · `docs/adr/*.md` — або «не виявлено». Словник: N слів заборон · N збігів в обсязі — або «словника немає».

## Тести

`swift test`, N тестів у `Tests/…`. Або «тестів немає».

---

## C1 · <назва>

Контракт: `SpatialPolicyTests.swift` 12 · `StandbyPolicyTests.swift` 6. Поведінки: після вимкнення тап зупиняється, рендерер лишається теплим · …

| id | кошик | де | зараз | пропоную | чому: connascence | розходиться з | стан |
|---|---|---|---|---|---|---|---|
| C1.1 | слова | R1 · `AppModel.swift:42` | `refresh()` | `readSystemState()` + `reconcileSessions()`, 2 → 4 | Meaning ×5: місця виклику не знають, що буде звіряння | — | |
| C1.2 | події | R1 · `AppModel.swift:118` | `isLive = false` + лог | «застосунок замовк на виході», збирається у `AppModel.stop()` | Meaning ×3, 2 модулі: `MenuView.swift:40`, `App.swift:22` | — | |
| C1.3 | правила | R1, R2 · `SpatialPolicy.swift:30`, `MenuView.swift:77` | `if app.isEligible && !standby` | тип `EligibilityRule`, дім `SpatialPolicy`; «невідомо» → відмова | Algorithm ×2, 2 модулі; гілка «невідомо» дозвільна | `CONTEXT.md:14 «Wanted»` — сходинка 2 | |
| C1.4 | склад | R1, R2 · `AppModel.swift` | розібрати: `SessionReconciler` окремо; перебудова | Honest and Complete Name має 3 «і»: тримає стан UI і звіряє сесії і володіє дозволом | — | потрібен контекст C2 |

Не дійшов: — або перелік файлів.

Стан: порожньо — чекає · `так` · `ні` · `змінено: …` · `виконано` · `пропущено: …` · `без відповіді`.
