# Лог restrukt: <проєкт>

## <дата час> · C1 · шлях B

виконано 12 із 14 · пропущено 2 · тести 104 → 111 · час: слова 3 хв · події 2 · правила 4 · збірка і тести 2 · коміт: `C1: 9 імен, 2 події, 1 правило`

| id | що зроблено | файли |
|---|---|---|
| C1.1 | `refresh()` → `readSystemState()` + `reconcileSessions()` | `AppModel.swift`, `MenuView.swift` |

| id | пропущено, бо |
|---|---|
| C1.4 | потрібен кластер C2 |

Контракт: після вимкнення тап зупиняється, рендерер лишається теплим — тримається.
