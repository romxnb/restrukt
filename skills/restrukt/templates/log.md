# Лог restrukt: <проєкт>

## <дата час> · C1a · R1 · зелений зріз

основа: commit збігається; початкові зміни користувача збережені · зріз: 3 рядки · виконано 3 · підтверджено поза зрізом 11 · швидкий контракт 104 → 107 · повний suite: очікує завершення C1a · час: правки 7 хв · перевірки й лог 4 · рекомендований коміт: `R1: expose order placement route`

| id | що зроблено | файли |
|---|---|---|
| C1a.1 | `check()` → `reserveStock()` | `OrderService.ts`, `CartView.ts` |

| id | пропущено, бо |
|---|---|
| C1a.4 | потрібен контекст C2 |

Контракт: після скасування резерв знімається, оплата повертається — тримається.

Трасованість: R1 має orchestration home `PlaceOrder.execute()`; `orderPlaced → reserveStock → FreeShippingRule → paymentCaptured → releaseToWarehouse` читається в причинному порядку; правила резерву й доставки делеговані доменним модулям.

DoD зрізу: швидкий контракт зелений · рядки виконані або пояснено пропущені · план і лог оновлені · початкові зміни користувача збережені.

DoD C1a: очікує 11 підтверджених рядків і повного suite.

To-be R1: `orderPlaced` → `reserveStock()` → `FreeShippingRule` → `paymentCaptured` → `Warehouse.receive()`.
