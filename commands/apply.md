---
description: restrukt, етап 3 — один зелений зріз узгодженого плану до семантичної трасованості; швидкий контракт, збірка, лог
argument-hint: "[контекст або підконтекст, наприклад C1 чи C1a; без аргументу — усі з рядками «так»]"
---

Виконай план restrukt. Контексти: $1

Прочитай `docs/restrukt/plan.md`: шапку, «Контексти», «Тести». Немає плану — запропонуй `/restrukt:scan`. Далі не йди. Немає жодного рядка `так` або `змінено` — проведи узгодження за `references/qa.md`, серіями `AskUserQuestion`.

Прочитай `${CLAUDE_PLUGIN_ROOT}/skills/restrukt/references/routes.md`, `apply.md`, `buckets.md`, `qa.md` і `templates/log.md`.

## 1 · Виконавці

`date '+%H:%M:%S'`. Для кожного обраного незалежного контексту запусти `restrukt-builder` на **один зелений зріз**; залежні контексти — по черзі. Кожному: шлях до плану, id контексту, швидкий контракт, повний suite і абсолютні шляхи до `${CLAUDE_PLUGIN_ROOT}/skills/restrukt/references/routes.md`, `apply.md`, `buckets.md`, `templates/log.md`.

## 2 · Показ

`date`. З відповідей виконавців, без переказу: зріз і залишок підтверджених рядків, тести до → після, повний suite або `очікує`, контракт, orchestration home і трасованість, змінені файли, повідомлення коміта. Час.

Комітить користувач. Лишились підтверджені рядки — `/restrukt:apply Cn` запускає наступний зелений зріз.

Якщо `${CLAUDE_PLUGIN_ROOT}` не розкрився — ті самі файли через скіл `restrukt`.
